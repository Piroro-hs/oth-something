module.exports = function random({placeables}) {
  return {
    compute({history: [{board, turn}]}) {
      const placeablesArray = placeables(board, turn);
      return Promise.resolve(placeablesArray[Math.floor(Math.random() * placeablesArray.length)]);
    },
  };
};
